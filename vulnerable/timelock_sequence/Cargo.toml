[package]
name = "timelock-sequence"
version = "0.1.0"
edition = "2021"
description = "Vulnerable Soroban contract demonstrating missing unlock ledger monotonicity checks"
license = "MIT"
repository = "https://github.com/Veritas-Vaults-Network/soroban-guard-contracts"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
