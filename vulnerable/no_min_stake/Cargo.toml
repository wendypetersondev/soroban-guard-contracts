[package]
name = "no-min-stake"
version = "0.1.0"
edition = "2021"
description = "Vulnerable Soroban staking contract that accepts micro-stakes and allows storage pollution"
license = "MIT"
repository = "https://github.com/Veritas-Vaults-Network/soroban-guard-contracts"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
