[package]
name = "storage-namespace-omitted"
version = "0.1.0"
edition = "2021"
description = "Vulnerable Soroban fixture: unrelated modules share the same storage symbol key"
license = "MIT"
repository = "https://github.com/Veritas-Vaults-Network/soroban-guard-contracts"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
