[package]
name = "even-median-bias"
version = "0.1.0"
edition = "2021"
description = "Vulnerable Soroban contract: even-number median picks lower price without manipulation guard"
license = "MIT"
repository = "https://github.com/Veritas-Vaults-Network/soroban-guard-contracts"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
