[package]
name = "rescue-primary-asset"
version = "0.1.0"
edition = "2021"
description = "Vulnerable Soroban contract demonstrating rescue function that can drain primary protocol asset"
license = "MIT"
repository = "https://github.com/Veritas-Vaults-Network/soroban-guard-contracts"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
