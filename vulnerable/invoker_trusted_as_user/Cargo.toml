[package]
name = "invoker-trusted-as-user"
version = "0.1.0"
edition = "2021"
description = "Vulnerable Soroban contract: cross-contract callback trusts invoker as original user"
license = "MIT"
repository = "https://github.com/Veritas-Vaults-Network/soroban-guard-contracts"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
